# Dockerfile for {{.AppName}}

FROM {{.BaseImage}}

WORKDIR {{.WorkDir}}

# Copy application files
COPY . .

# Set environment variables
{{range .Env}}
{{.}}
{{end}}

# Expose port
EXPOSE {{.Port}}

# Run the application
CMD {{range $i, $cmd := .Cmd}}{{if $i}} {{end}}"{{$cmd}}"{{end}}